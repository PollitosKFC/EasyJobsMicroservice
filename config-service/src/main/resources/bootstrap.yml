server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PollitosKFC/EasyJobsMicroservice.git
          searchPaths: config-data
          user-name:
          password:
