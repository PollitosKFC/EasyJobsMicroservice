spring:
  application:
    name: Accounts
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
boot:
  admin:
    client:
        serviceUrl:
            defaultZone: http://localhost:8086