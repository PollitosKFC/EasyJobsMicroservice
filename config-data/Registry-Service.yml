server:
  port: 8099

eureka:
  instance:
    hostname: ec2-54-221-134-209.compute-1.amazonaws.com

  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: "https://${eureka.instance.hostname}:${server.port}/eureka/"#Endpoint para default zone